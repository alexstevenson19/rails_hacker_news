<%= form_for :comment, url: [article, :comments] do |f| %>

  <% if @comment && @comment.errors.any? %>
    <div>
      <h2> <%= pluralize(@comment.errors.count, "error") %> prohibited this article from being saved:
      </h2>
      <ul>
        <% @comment.errors.full_messages.each do |message| %>
          <li> <%= message %> </li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <p>
    <%= f.label :content %></br>
    <%= f.text_area :content %>
  </p>
  <p>
    <%= f.submit %>
  </p>
<% end %>

